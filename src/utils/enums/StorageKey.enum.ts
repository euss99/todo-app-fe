/**
 * Enum representing the keys used in localStorage
 */
enum StorageKey {
  /** Key for storing the application theme preference */
  THEME = "theme",
  AUTH_TOKEN = "auth_token"
}

export default StorageKey;